<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Titan Battle!</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
            background-image: url('/paradise.webp');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            color: #e0e1dd;
            min-height: 100vh;
        }
        .container {
            background: rgba(27, 38, 59, 0.95);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.5);
            border: 2px solid #415a77;
            backdrop-filter: blur(5px);
        }
        h1 {
            color: #e0e1dd;
            text-shadow: 0 0 10px #ff6b6b;
            font-size: 2.5em;
            margin-bottom: 20px;
            text-align: center;
        }
        .message {
            padding: 25px;
            margin: 25px 0;
            border-radius: 10px;
            font-size: 20px;
            font-weight: bold;
        }
        .lost {
            background-color: #8b0000;
            color: #ffb3b3;
            border: 3px solid #ff6b6b;
        }
        .won {
            background: linear-gradient(45deg, #2e8b57, #32cd32);
            color: #ffffff;
            border: 3px solid #00ff00;
            text-shadow: 0 0 10px #00ff00;
        }
        .ready {
            background-color: #1e3a5f;
            color: #a9b6c9;
            border: 3px solid #415a77;
        }
        .awaken {
            background: linear-gradient(45deg, #4b0082, #8a2be2);
            color: #e6ccff;
            border: 3px solid #9370db;
            text-shadow: 0 0 10px #9370db;
        }
        .flag {
            font-size: 28px;
            font-weight: bold;
            color: #ffd700;
            margin: 30px 0;
            padding: 20px;
            background: rgba(139, 0, 0, 0.3);
            border-radius: 10px;
            border: 3px solid #ffd700;
            text-shadow: 0 0 10px #ff6b6b;
        }
        button {
            background: linear-gradient(45deg, #ff6b6b, #ff8e8e);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            font-weight: bold;
            margin: 15px;
            transition: all 0.3s;
            text-align: center;
            
        }
        button:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(255, 107, 107, 0.4);
        }
        .titan-display {
            margin: 60px ;
            display: grid;
            place-items: center;
        }
        .titan-image {
            width: 200px;
            height: 200px;
            object-fit: cover;
            border-radius: 10px;
            border: 3px solid #415a77;
            box-shadow: 0 0 20px rgba(255, 107, 107, 0.5);
        }
        .hint {
            margin-top: 30px;
            padding: 20px;
            background-color: #2a3c5a;
            border: 2px solid #ffd700;
            border-radius: 10px;
            color: #ffd700;
            font-size: 16px;
        }
        .debug-info {
            margin-top: 25px;
            padding: 15px;
            background-color: #2a3c5a;
            border: 1px solid #415a77;
            border-radius: 5px;
            font-family: monospace;
            font-size: 12px;
            text-align: left;
        }
        .fight-animation {
            animation: fight 0.6s ease-in-out;
        }
        @keyframes fight {
            0% { transform: scale(1); }
            25% { transform: scale(1.4) rotate(-5deg); }
            50% { transform: scale(1.2) rotate(5deg); }
            75% { transform: scale(1.3) rotate(-3deg); }
            100% { transform: scale(1); }
        }
        form {
            display: inline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>⚔️ TITAN BATTLE ARENA ⚔️</h1>
        
        <div class="titan-display" id="titanDisplay">
            <% if (titan === 'Attack Titan') { %>
                <img src="/Attack Titan.jpg" alt="Attack Titan" class="titan-image">
            <% } else if (titan === 'Colossus Titan') { %>
                <img src="/Colossus Titan.png" alt="Colossus Titan" class="titan-image">
            <% } else if (titan === 'Armored Titan') { %>
                <img src="/Armored Titan.jpg" alt="Armored Titan" class="titan-image">
            <% } else if (titan === 'Female Titan') { %>
                <img src="/Female Titan.jpg" alt="Female Titan" class="titan-image">
            <% } else if (titan === 'Beast Titan') { %>
                <img src="/Beast Titan.jpg" alt="Beast Titan" class="titan-image">
            <% } else if (titan === 'Jaw Titan') { %>
                <img src="/Jaw Titan.webp" alt="Jaw Titan" class="titan-image">
            <% } else if (titan === 'Cart Titan') { %>
                <img src="/Cart Titan.jpg" alt="Cart Titan" class="titan-image">
            <% } else if (titan === 'War Hammer Titan') { %>
                <img src="/War Hammer Titan.webp" alt="War Hammer Titan" class="titan-image">
            <% } else if (titan === 'Founding Titan') { %>
                <img src="/Founding Titan.jpg" alt="Founding Titan" class="titan-image">
            <% } else { %>
                <div style="font-size: 100px;">❓</div>
            <% } %>
        </div>

        <div class="message <%= won ? 'won' : (message.includes('awakens') ? 'awaken' : (message.includes('ready') ? 'ready' : 'lost')) %>">
            <%= message %>
        </div>

        <% if (won) { %>
            <div class="flag">
                🏆 VICTORY: <%= flag %> 🏆
            </div>
        <% } %>

        <form id="fightForm" method="POST" action="/fight">
            <button type="submit">Engage in Battle!</button>
        </form>
        
        <button onclick="resetGame()">Choose Another Titan</button>

        <% if (!won && !message.includes('ready') && !message.includes('awakens')) { %>
            <div class="hint">
                💡 Hint: The Nine Titans are powerful, but there exists one above all... 
                <br>The Founding Titan controls the very fabric of reality. 
                <br>Perhaps the verification method has a weakness in its foundation...
            </div>
        <% } %>

        <div class="debug-info">
            <strong>Battle Intel:</strong><br>
            Current Titan: <%= titan %><br>
            Status: <%= message %>
        </div>
    </div>

    <script>
        // Add animation when form is submitted
        document.getElementById('fightForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const titanDisplay = document.getElementById('titanDisplay');
            titanDisplay.classList.add('fight-animation');
            
            // Remove animation class after it completes
            setTimeout(() => {
                titanDisplay.classList.remove('fight-animation');
                // Submit the form after animation
                setTimeout(() => {
                    this.submit();
                }, 200);
            }, 600);
        });

        function resetGame() {
            document.cookie = 'titanToken=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;';
            location.href = '/';
        }

        // Log token info to console for participants
        console.log('Titan Token Cookie:', document.cookie);

    </script>
</body>
</html>